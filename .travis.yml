language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "zy"
  - git config user.email "uljjmhn520@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${gitpage_hexo}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/uljjmhn520/uljjmhn520.github.io.git
